import { files } from './files';
import { media } from './media';
import { numbers } from './numbers';
import { text } from './text';

export const Mocker = {
  media,
  files,
  text,
  numbers,
  entities: {},
};
